apiVersion: v1
kind: Service
metadata:
  name: eks-deployment
  namespace: eks-deployment
  labels:
    app: eks-deployment
spec:
  selector:
    app: eks-deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080